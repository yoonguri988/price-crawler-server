services:
  - type: web
    name: crawl-server
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: RENDER
        value: "true"
